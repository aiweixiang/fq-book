# 翻墙（科学上网）方法论技术文档

> 本文档是对《这本书能让你连接互联网》（fq-book）全书内容的系统化提炼，抽取其中关于「绕过网络审查（翻墙）」的**方法论与原理**，而非具体软件的重复操作步骤。
> 原书允许演绎与共享、禁止商用；本文档仅供科研与学习，相关风险请见文末免责声明。

---

## 目录

1. [一句话总结](#一句话总结)
2. [总体框架：三层模型](#总体框架三层模型)
3. [墙是如何工作的（技术原理）](#墙是如何工作的技术原理)
4. [绕过方法论：核心抽象](#绕过方法论核心抽象)
5. [技术手段全景分类](#技术手段全景分类)
6. [冷启动方法论：先有工具，再谈使用](#冷启动方法论先有工具再谈使用)
7. [稳定使用与账号体系](#稳定使用与账号体系)
8. [故障排查方法论](#故障排查方法论)
9. [抗封锁设计原则](#抗封锁设计原则)
10. [安全与合规注意事项](#安全与合规注意事项)

---

## 一句话总结

「翻墙」的本质是：**在客户端与目标服务器之间建立一条墙无法（或代价过高而不敢）识破的间接通道**。方法论的核心不是记住某个软件，而是掌握三条主线：

1. **加密**——让墙看不懂内容；
2. **混淆/伪装**——让墙认不出这是"翻墙流量"；
3. **提高封锁成本**——借用云厂商 IP、CDN、P2P 等"打不起/不敢打"的目标。

三者叠加，再辅以**多路冗余**，才是长期可用的完整方案。

---

## 总体框架：三层模型

翻墙全过程可抽象为三个阶段，缺一不可：

| 阶段 | 目标 | 典型手段 |
|------|------|----------|
| **冷启动** | 在"什么都没有"的情况下拿到第一个可用工具/节点 | 搜索引擎、GitHub、镜像站、邮件自动回复、Web 代理、hosts/DNS、P2P、社会工程 |
| **稳定使用** | 建立日常可用的通道 | SS/SSR/V2Ray、VPN、Clash 客户端、订阅、自建服务器 |
| **纵深防御** | 单一方式被封后不断网 | 多协议、多节点、前置中转、去中心化（Tor/IPFS）、纯净 IP + 指纹浏览器 |

> 原书反复强调：**"随时面临断网的单一上网方式已经不能满足需要"**。有效结合多种工具、组成互相连接的节点，才不容易断网。

---

## 墙是如何工作的（技术原理）

> 详见原书 `docs/abc/gfw.md`、`docs/abc/4dns.md`、`docs/abc/dc_zhenshu.md`、`docs/abc/1190000011485579.md`。

**核心认知：GFW 本质是一个巨大的高性能分布式入侵检测（IDS）系统，而非单纯的防火墙。** 它双向过滤境内外的信息访问。主要封锁手段有五类：

### 1. 关键字过滤阻断（针对明文）
- 主要针对 HTTP 明文流量（80 端口）。
- 若关键词出现在 HTTP 请求头（如 `Host: www.youtube.com`）或数据流正文中，GFW 会**伪装成通信双方发送伪造的 RST 包**，重置 TCP 连接。
- 阻断发生后，同一 IP 对同一目标通常会被临时封禁约 90 秒。

### 2. IP 地址封锁（路由黑洞）
- 在通往境外的"最后一跳"网关上**注入伪造路由规则**，使目标 IP 的数据包被丢弃。
- 副作用：多域名共用一个 IP 的虚拟主机/托管服务会被"误伤"整段 IP。

### 3. 特定端口封锁
- 丢弃特定 IP 上特定端口的数据包（如 SSH 22、PPTP 1723、SSL 443）。
- 更阴险的是**分时段间歇封锁**（如对 Google 443 端口"连 10 分钟、断 10 分钟"），让问题看起来像是服务商自己的故障。

### 4. SSL 连接阻断（SNI 识别 + TCP 重置）
- HTTPS 内容虽不可破译，但 **SNI（服务器名称指示）字段是明文的**，暴露了访问域名。
- GFW 识别 SNI 后，用伪造 RST 包打断 TLS 握手，出现 `ERR_CONNECTION_RESET`。
- 对抗方向：域前置（Domain Fronting）、ESNI/ECH 加密 SNI。

### 5. DNS 劫持与污染
- **劫持**：ISP 在 DNS 服务器里写入虚假解析记录（如把 Google 域名解析到百度）。
- **污染**：在 UDP 53 端口上做入侵检测，发现敏感域名请求就**伪装成目标域名服务器返回虚假 IP**。
- 关键结论：**改公共 DNS 只能防"劫持"，防不了"污染"**——因为无论用国内还是国外 DNS，解析结果都要穿过 GFW，都会被污染。真正的解法是 DNS 加密（DNSCrypt/DoH/DoT）或 hosts 直连。

### 6. 进阶：流量特征识别（机器学习）
- 除上述手段外，GFW 会**先识别 SSL/SSH 首部特征，再统计包长度与方向，用训练好的模型判断承载流量是否像 HTTP**。
- 含义：**光加密不够，流量"长得像翻墙"也会被封**——这解释了为什么混淆和伪装如此重要。

---

## 绕过方法论：核心抽象

### 核心矛盾
- **加密 vs 识别**：加密让内容不可见，但协议/流量特征仍可被识别。
- **隐蔽 vs 稳定**：越隐蔽越慢，越快越容易被识别，需权衡。

### 三种基本范式

| 范式 | 工作层次 | 特点 | 代表 |
|------|----------|------|------|
| **代理（Proxy）** | 应用层/会话层 | 只转发特定应用的数据包，需逐应用配置 | HTTP 代理、SOCKS5、SS/SSR、V2Ray |
| **隧道/VPN** | 网络层/链路层 | 虚拟网卡，全局接管所有流量 | PPTP、L2TP/IPSec、OpenVPN、WireGuard |
| **内容寻址/去中心化** | 应用层之上 | 绕开"中心化域名/服务器"这一封锁点 | IPFS、Tor、P2P、ZeroNet |

### 分层视角（OSI 模型）

```
应用层     HTTP 代理（明文、可缓存、可窃听）
表示层     浏览器扩展中继（如 SwitchyOmega）
会话层     SOCKS4/5（转发 TCP/UDP，支持认证）
传输层     TCP/UDP（VPN 隧道在此之下）
网络层     IP（VPN 虚拟网卡接管此层）
链路层     物理网络（路由器透明代理/梅林固件）
```

> **层次越低，接管越彻底**：VPN 在 IP 层工作，所以能代理所有协议；SOCKS 在会话层，只能代理支持 SOCKS 的软件——这就是"为什么 v2ray 需要 SwitchyOmega 在表示层做中继"的原因。

### 三个关键洞察

1. **加密解决"内容审查"，混淆解决"流量识别"**。SSH 隧道虽然加密，但特征明显，曾遭定向干扰；而"在翻墙连接里做些别的操作（如反复 tail 长文件）"就能让流量识别模型失效——这就是混淆的思想。
2. **端口、IP、域名都可以被封，但"看起来像合法流量"最难封**。域前置利用这一点：审查者要么放行所有看似无害的流量，要么误伤大量合法业务。
3. **提高封锁成本**。meek 网桥不直连自己服务器，而是借道 AWS/Azure 等云平台——云平台绑定大量国内业务，封杀这些 IP 代价极高，因此不敢封。

---

## 技术手段全景分类

> 各手段具体配置见原书对应章节（路径见文末索引）。

### 5.1 DNS 层（防污染/防劫持）
| 手段 | 能防什么 | 局限 |
|------|----------|------|
| 修改公共 DNS（阿里/腾讯/百度） | 防 ISP 劫持、证书错误 | 防不了 UDP 53 污染 |
| **DNSCrypt / DoH / DoT** | 防污染、防劫持 | 只解决 DNS，不解决封 IP |
| **hosts 文件** | 跳过 DNS 解析直连 IP | IP 被封就没辙；CDN IP 被封需真实 IP |
| 本地 DNS + dnsmasq + stunnel + sniproxy | 综合抗污染 | 需一定动手能力 |

> 用 `ipconfig/flushdns`（或系统缓存刷新）清理 DNS 缓存。hosts 优先级高于 DNS，但也会带来"代理开着仍上不了网"的排查难点（见第 8 节）。

### 5.2 代理层（Proxy）
- **HTTP 代理**：明文、可缓存、可被窃听/篡改，只能代理 HTTP。
- **SOCKS4/5**：会话层，SOCKS5 支持认证与 UDP 转发。
- **SSH 隧道**：`ssh -D 7001 user@host`，加密但特征明显，适合临时应急。
- **Shadowsocks（SS）/ SSR**：把 SOCKS5 拆成 local + server 两端，中间用可选的加密方法传输；SSR 增加协议与混淆插件。
- **V2Ray / Xray（VMess/VLESS/Trojan）**：多协议、可伪装（WebSocket、TLS、mKCP 等），是目前自建的主流。
- **前置（二级）代理**：用一个快速节点中转去目标节点，加速慢速线路。
- **透明代理**：装在路由器上，对终端"透明"。

### 5.3 VPN / 隧道层
| 协议 | 易用性 | 速度 | 安全性 | 网络适用性 |
|------|--------|------|--------|------------|
| PPTP | 高 | 快 | 低 | 已被大量封杀 |
| L2TP/IPSec | 中 | 中 | 中 | 适合局域网受限环境 |
| OpenVPN | 低 | 中 | 高 | 应用层，可穿透多数 NAT |
| WireGuard | 中 | 快 | 高 | 现代方案，性能好 |

> 选择原则：**先看设备支持什么、网络是否阻塞该协议，再在安全与速度之间权衡**。OpenVPN/WireGuard 均已被特征识别重点关照，需结合混淆。

### 5.4 混淆与伪装
- 流量混淆插件（obfsproxy 等）：把翻墙流量伪装成普通流量。
- **meek**：把 Tor 流量伪装成访问云平台（AWS/Azure）的 HTTPS 流量。
- **域前置（Domain Fronting）**：DNS/SNI 用无害域名，真实域名在加密后发出。
- **ESNI/ECH**：加密 SNI，根治 SNI 识别。

### 5.5 网页代理 / 代理浏览器
- Web 代理原理：网页上输入网址，由代理服务器代为抓取并返回。
- 优点：零配置；缺点：不完整加载脚本，可嵌套多个 Web 代理解决。
- 代理浏览器（Tor、Puffin、Opera、内置外网系统的云桌面如 rabb.it/demo.glyptodon）。

### 5.6 去中心化
- **Tor**：强加密 + 多跳，配合 meek 网桥可突破封锁；用于匿名与暗网。
- **IPFS**：内容寻址、分布式存储，理论上"不受防火墙影响"，文件永久可寻。
- **P2P（eMule 等）**：加密传输 + 分布式，难以被敏感词过滤。
- **ZeroNet**：分布式网站。

### 5.7 自建（Self-host）
- 核心思路：国内到部分境外地区（如台湾）互通，可买最便宜的台湾服务器作中转跳板。
- 典型栈：云服务器 + `shadowsocks-libev` 服务端 + Clash 客户端（Rule 模式分流国内外流量）。
- 优势：独立 IP、可控；劣势：单一 VPS 特征明显、易被封，需定期换端口/节点。

---

## 冷启动方法论：先有工具，再谈使用

> 这是原书 `docs/method/get-method.md` 的核心章节——"获取梯子上网的方式"。**关键在"留心"，即信息获取能力本身。**

完整链路（按可行度排序）：

1. **搜索引擎**：用 qwant 搜翻墙软件关键词，配合 similarsitesearch 找相似站点。
2. **GitHub 搜索**：搜 `vpn`、`v2ray`、`ss/ssr分享` 等关键词，GitHub 熟练使用是重中之重。
3. **已有工具滚雪球**：用能用的 VPN/代理继续去 Google 下载更多工具。
4. **邮件自动回复**：如向 `get@psiphon3.com` 发标题 `help` 的邮件，自动回复工具/镜像地址。
5. **镜像站**：打开分享站镜像下载资源。
6. **Web 代理 / 代理浏览器**：先翻出去，再获取正规工具。
7. **浏览器扩展下载站**：离线下载代理插件（如 chrome-extension-downloader）。
8. **修改 hosts / DNS**：把 SS 分享站、VPN 官网的 IP 加入 hosts，或开 DNSCrypt 打开被污染站点。
9. **P2P 下载**：连 eMule 服务器搜索 tor 等工具。
10. **移动设备**：把安卓地区/语言改成新加坡等，共享 PC 网络后从 Google 商店下载代理软件。
11. **个人博客 / 视频平台**：博主和 UP 主常分享免费节点，用临时邮箱注册即可。
12. **社会工程学**：找外企/国外/技术宅朋友要一份（压缩加密，防深度包检测）。
13. **Telegram 频道订阅**：用内置代理壳的软件订阅免费节点频道。
14. **战略家模式**：不依赖单个工具，而是**自己收集资料 + 数据整理分析（word/excel/mind/uml）+ 实践论证**，形成自己的情报体系。

> 方法论精髓：**把这些方式组合成"互相连接的节点"**——例如"用 Web 代理/DNSCrypt 打开 SS 站 → 用 SS 翻出去 → 再下载其他 VPN/代理"，形成闭环，就不会轻易断网。

---

## 稳定使用与账号体系

拿到通道只是第一步，长期稳定使用还需要"身份"与"支付"体系（原书 `docs/fkca/` 系列）：

### 纯净 IP
- 纯净度排序：**原生住宅 IP > 静态住宅 IP > 动态住宅 IP > 移动 IP > 数据中心 IP**。
- 检测：IPQualityScore、Scamalytics、AbuseIPDB（欺诈分 < 25 较安全）、ping0.cc。
- 应用：跨境电商、社媒运营防关联、爬虫、广告投放、API 访问。

### 指纹浏览器 + 代理 IP
- 对地理位置敏感的应用（Claude、PayPal）而言，"IP 看起来像真人"比"速度快"更重要。
- 组合：静态住宅 ISP 代理 + 指纹浏览器（如 HubStudio）+ Clash TUN 模式。

### 国外手机号
- eSIM 方案（kitesim、X-esim + esimplus）或实体卡（giffgaff 经台湾中转）。
- 用于注册 Google、Apple 美区 ID、ChatGPT 等验证。

### 虚拟信用卡
- 开卡链路：Binpay + 虚拟币交易所（欧易）+ 美国地址生成器（选免税州）。
- 用于订阅海外服务、Cloudflare 等平台付款。

### 美区 Apple ID + 礼品卡
- 自注册美区 ID（避免账号被盗），用官方礼品卡充值购买 Shadowrocket 等客户端。

---

## 故障排查方法论

> 原书 `docs/a123/rst-syscall.md`、`docs/abc/dc_zhenshu.md`、`docs/append/block-ip.md` 提供了排查框架：**杜绝主观情绪化 → 多角度考虑可能 → 建立关联找共同点**。

| 症状 | 可能原因 | 对策 |
|------|----------|------|
| 能 ping 通，但网页打不开 | DNS 污染 / SNI 被 RST | 换 DNS、开 DNSCrypt、检查 hosts |
| 开了代理仍上不了网 | hosts 映射了错误 IP；远程 DNS 仍走本地 | 清 hosts、改国外 DNS、刷新 DNS 缓存 |
| `ERR_CONNECTION_RESET` | TCP 重置攻击（SNI 识别） | 域前置、加密 SNI、换协议 |
| `ERR_CERT_AUTHORITY_INVALID` | ISP 的 DNS 返回错误根证书 | 换第三方 DNS + flushdns |
| 端口被封 ≠ IP 被封 | 只封 80/443 等特定端口 | 换端口；IP 被封则需换 IP |
| 代理软件报乱码 | 安装路径含空格/特殊字符 | 移到无空格路径 |

检测工具：viewdns.info/chinesefirewall、vps234.com/ipchecker、itdog.cn、ping.pe 等。

---

## 抗封锁设计原则

1. **冗余与多路**：永远不要单点依赖一个节点/协议；订阅源、镜像、多协议并行。
2. **低成本试错、快速切换**：节点失效是常态，靠订阅自动更新 + 快速切换。
3. **提高封锁成本**：借用云厂商 IP、CDN、P2P、meek 等"不敢封/封不起"的目标。
4. **加密 + 混淆 + 伪装三位一体**：单有加密必被特征识别，单有混淆必被内容分析。
5. **层次分离**：代理走应用层、VPN 走网络层、路由器透明代理走链路层，按需组合。
6. **身份与链路解耦**：纯净 IP + 指纹浏览器把"网络身份"与"真实身份"隔离。

---

## 安全与合规注意事项

### 安全风险
- **免费节点/站点可能挖矿**：访问异常卡顿且非网速原因时，立即关闭并弃用。
- **钓鱼与蜜罐**：Tor 出口节点可能是蜜罐；反向代理梯子可能篡改链接，务必用可信服务或自建。
- **共享 IP 风险**：万人骑 IP 会导致账号批量封禁、触发验证码、被黑名单。
- **国产杀软可能恶意禁用翻墙服务**：注意排查安全软件干扰。

### 合规免责（原书声明）
- 本书面向海外华人用户，**仅供科研与学习**，切勿用于其他用途。
- 相关行为在部分司法辖区可能违法，使用者需自行评估并承担后果。
- 本文档仅做技术原理与方法的归纳整理，不构成任何操作建议。

---

## 附：原书章节索引（本文档素材来源）

| 主题 | 路径 |
|------|------|
| 获取梯子的方式（方法论） | `docs/method/get-method.md` |
| 快速开始 | `docs/qintro/quickstart.md` |
| GFW 原理和封锁技术 | `docs/abc/gfw.md` |
| 域名劫持与污染 | `docs/abc/4dns.md` |
| 数字证书攻防 / SNI / 域前置 | `docs/abc/dc_zhenshu.md` |
| 科学上网原理（SSH/VPN/SS/PAC） | `docs/abc/1190000011485579.md` |
| 正向/反向/透明代理 | `docs/abc/proxy-s-x.md` |
| VPN 隧道协议 | `docs/abc/4vpn.md` |
| meek 网桥原理 | `docs/abc/meek.md` |
| 软件互联协议机制 | `docs/abc/connection.md` |
| 代理上网准入检测实验 | `docs/abc/web-proxy-x.md` |
| 纯净 IP | `docs/abc/pure.md` |
| NAT 类型 | `docs/abc/4nat.md` |
| GitHub RST 与 SSL_ERR_SYSCALL | `docs/a123/rst-syscall.md` |
| 深网与暗网 | `docs/abc/darkweb.md` |
| SS/SSR/V2Ray 链接解析 | `docs/append/srvurl.md` |
| 封锁 IP | `docs/append/block-ip.md` |
| SS 二级（前置）代理 | `docs/append/ss-relay.md` |
| 自建云梯（SS 服务端 + Clash） | `docs/fkca/ss-server.md` |
| 代理 IP + 指纹浏览器 | `docs/fkca/fxxkcxxxa.md` |
| 获取国外手机号 | `docs/fkca/esim.md` |
| 虚拟信用卡 | `docs/fkca/mastercard.md` |
| 美区 ID 购买小火箭 | `docs/fkca/apple-ssr.md` |
| IPFS / 去中心化 | `docs/ipfs/ipfs.md`、`docs/ipfs/why.md` |
| Tor / 代理浏览器 | `docs/browse/tor-firefox.md` |
| 网页代理 | `docs/web/web-proxy.md` |

---

*本文档由对 fq-book 全书的系统化 review 生成，抽取方法论与原理，去除冗余的软件操作步骤与失效链接。*
